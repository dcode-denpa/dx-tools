const s = require('storyeng')
const should = require('should')

describe('impall', () => {

  it( 'should dynamically require modules', done => {
    s.impall(`${__dirname}/testparts`)
    should(foo).equal('bar')
    should(some).equal('thing')
    done()
  })

})