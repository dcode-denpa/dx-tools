const s = require('storyeng')
const should = require('should')

const long = `
Here is a super duper long line that should be well over the default of about 70 characters are we there yet?
`

const wrapped = 
`Here is a super duper long line that should be well over the default 
of about 70 characters are we there yet?
`

describe('tell', () => {
  console.log(s.tells(long))
  it( 'should wrap', done => {
    should(s.tells(long)).equal(wrapped)
    done()
  })

})