/// <reference types="node" />
import EventEmitter from 'events';
import { CountryCode, GtcOptions, GtcResult } from '../types/gtc';
declare class Gtc extends EventEmitter {
    #private;
    constructor(options: GtcOptions);
    init(): Promise<void>;
    find(countryCode: CountryCode, phoneNumber: number | string): Promise<GtcResult | null>;
}
export default Gtc;
